import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.newbeginning.intake',
  appName: 'New Beginning Client Portal',
  webDir: 'out'
};

export default config;
