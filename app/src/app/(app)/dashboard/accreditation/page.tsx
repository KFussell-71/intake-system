'use client';

import { AccreditationManager } from '@/features/accreditation/components/AccreditationManager';

export default function AccreditationPage() {
    return (
        <div className="h-[calc(100vh-100px)] p-6">
            <AccreditationManager />
        </div>
    );
}
